serverVersion={},serverVersion.major=1,serverVersion.minor=0,serverVersion.patch=2,function(){let e=Object.keys(serverVersion),r="";for(i=0;i<3;i++)r+=serverVersion[e[i]],2!=i&&(r+=".");serverVersion.version=r}();