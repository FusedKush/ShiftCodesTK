<?php
  namespace ShiftCodesTK\Forms\FormCore;

  /** Represents the *Properties* that define Forms and their children. */
  class Properties {
    use Properties\StateManager;

    /** Initialize the `Properties` subclass. */
    public function __construct () {
      parent::__construct();
    }
  }
?>