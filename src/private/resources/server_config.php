<?php
  /** Represents the configurable properties of ShiftCodesTK.
   * 
   * You can use `ShiftCodesTK\SERVER_CONFIGURATION` to access these properties at runtime.
   * 
   */
  return [
    /** @var string A version number representing the current version of ShiftCodesTK. */
    "version"        => "1.5.0",
    /** @var bool Indicates if site-wide *Maintenance Mode* is currently active or not. */
    "in_maintenance" => false
  ];
?>